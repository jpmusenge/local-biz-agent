# Google Places API
# Required for business discovery. Get your API key from:
# https://console.cloud.google.com/apis/credentials
# Enable these APIs: Places API, Geocoding API
# Note: If not set, the discovery module runs in MOCK MODE with fake data
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# Anthropic Claude API (for website generation)
# Required for AI-powered website generation. Get your API key from:
# https://console.anthropic.com/settings/keys
# Note: If not set, the generator module runs in MOCK MODE with sample templates
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI API (optional, for future use)
OPENAI_API_KEY=your_openai_api_key_here

# Vercel Deployment
# Get your token from: https://vercel.com/account/tokens
# Note: If not set, the deployment module runs in MOCK MODE with fake URLs
VERCEL_TOKEN=your_vercel_token_here

# Optional: Team ID for team deployments (leave blank for personal account)
# Find it in your team settings URL: https://vercel.com/teams/{team-slug}/settings
VERCEL_TEAM_ID=

# Base domain for deployed websites (subdomains will be: {business-name}-v1.{domain})
# Example: mybizwebsites.com -> marcus-barbershop-v1.mybizwebsites.com
# Leave as "vercel.app" to use Vercel's default domain
DEPLOYMENT_DOMAIN=vercel.app

# Legacy - kept for backwards compatibility
VERCEL_ORG_ID=your_vercel_org_id_here
VERCEL_PROJECT_ID=your_vercel_project_id_here

# Email Configuration (future use)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_smtp_user_here
SMTP_PASS=your_smtp_password_here
EMAIL_FROM=noreply@yourdomain.com

# Database
DATABASE_PATH=./data/local-biz.db

# Debug mode (set to "true" for verbose logging)
DEBUG=false
